<Project Sdk="Microsoft.NET.Sdk.Worker">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>dotnet-Niuro.Worker-b829666c-fc2b-4b7d-94b2-cf4a1267eef4</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <!-- Con Central Package Management las versiones viven en Directory.Packages.props -->
    <PackageReference Include="Microsoft.Extensions.Hosting" />
    <PackageReference Include="Serilog.Extensions.Hosting" />
    <PackageReference Include="Serilog.Sinks.File" />
    <!-- Provider EF Core: el worker procesa el outbox con el DbContext; referencia directa para
         unificar la versión de EF Core con core/ (vía ProjectReference se resolvía a la mínima). -->
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\core\Niuro.Core.csproj" />
  </ItemGroup>
</Project>
